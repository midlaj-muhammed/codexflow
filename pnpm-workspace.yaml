packages:
  - apps/*
  - packages/*

# `unrs-resolver` is an indirect tooling dependency. Its postinstall may download a
# native binding, so Phase 0 explicitly denies that build step rather than allowing
# arbitrary dependency scripts. The package has a JavaScript fallback for tooling.
allowBuilds:
  unrs-resolver: false
